// Sample JSON object
let json = {
    key1: 'value1',
    key2: 'value2'
};

// Initializing an empty array to hold JSON objects
let jsonArray = [];

// Pushing the JSON object into the array
jsonArray.push(json);
